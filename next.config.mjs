/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.tapology.com', 'www.tapology.com'], // Add any other image domains here
    },
  };
  
  export default nextConfig;
  